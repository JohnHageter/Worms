from Module.io.OpenCVCamera import OpenCVCamera as Camera
import cv2

# video = open_dataset("Data/mp4_data/Trial1_2.mp4")

# p = WellDetectionParams(
#     well_tolerance=0.15,
#     n_wells=6,
#     hough_dp=1.5,
#     min_dist=50,
#     hough_param1=24,
#     hough_param2=25,
#     min_radius=46,
#     max_radius=107,
# )

# wells, masks, _ = detect_wells_interactive(video, p)

# pprint(p)
# ret, frame = video.read()

# for x, y, r in wells:
#     cv2.circle(frame, (int(x), int(y)), int(r), (0, 0, 255), 1)

# cv2.imshow("wells", frame)
# cv2.waitKey(0)


